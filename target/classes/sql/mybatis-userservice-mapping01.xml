<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- SQL definition -->
<!-- MyBatis 에서 Collection, 자바 라이브러리 클래스들의 알리야스 사용
java.lang.String 은  string으로 사용
java.util.List 은 list 로 사용
java.util.ArrayList 는 arraylist로 사용
java.lang.Integer는 integer로 사용

2. 
parameterType="user" :  getter 로 뽑아내는 것 #{필드명}
parameterType="string"  : 단순 #{VALUE}
 -->

<mapper namespace="UserMapper01">
	<select id="getUser" parameterType="string" resultType="user">
		SELECT 
		user_id AS userId, user_name AS userName, password, age, grade, reg_date AS regDate
		FROM users
		WHERE user_id=#{VALUE}
	</select>
	
	<select id="getUserList" resultType="user">
		SELECT 
		user_id AS userId, user_name AS userName, password, age, grade, reg_date AS regDate
		FROM users
	</select>
	
	<select id="findUserId" parameterType= "user" resultType="string">
		SELECT
		user_name
		FROM users
		WHERE
		user_id=#{userId} AND password=#{password} 
	</select>
</mapper>